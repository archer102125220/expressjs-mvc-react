import system from '@models/client/system';
import userList from '@models/client/userList';
import videoList from '@models/client/videoList';

const pluginModels = [system, userList, videoList];

export default pluginModels;